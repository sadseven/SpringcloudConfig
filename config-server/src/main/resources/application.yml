server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sadseven/SpringcloudConfig
          searchPaths:  repo
          username: 
          password:
      label: master
